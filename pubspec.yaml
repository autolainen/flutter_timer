name: flutter_timer
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^1.2.4
  wave: ^0.0.8
  flutter_bloc: ^6.0.0

flutter:
  uses-material-design: true
